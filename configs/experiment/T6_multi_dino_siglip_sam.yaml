# T6 — Multi-teacher: DINOv2-L + SigLIP-L + SAM-L
# Adding SAM: boundary awareness, object segmentation priors
# Tests value of structural/boundary knowledge from SAM
# SAM features are 256d, need projection; dense 64x64 pooled to 16x16
# @package _global_
defaults:
  - /model/encoder: vit_large
  - /model/decoder: pixel_decoder_large
  - /teacher: multi_dino_siglip_sam
  - /alignment: relational_kd
  - /loss: standard
  - /training: tokenizer_full
  - _self_

exp_name: "T6_multi_dino_siglip_sam"
notes: "3-teacher: DINO+SigLIP+SAM. Compare vs T5 to measure SAM's contribution."
